defmodule WeatherStation.Components.TemperatureGauge do
  # use Scenic.Component

  # def validate({daily_high, current, daily_low}) when is_float(daily_high) and is_float(current) and is_float(daily_low) do
  #   {:ok, {daily_high, current, daily_low}}
  # end

  # def validate(data) do
  #   {
  #     :error,
  #     """
  #     #{IO.ANSI.red()}Invalid Temperature Gauge specification
  #     Received: #{inspect(data)}
  #     The data for the TemperatureGauge is `{float, float, float}`.#{IO.ANSI.default_color()}
  #     """
  #   }
  # end

  # def compile({daily_high, current, daily_low}) do

  # end

  # def contains_pont?()
end
